== Stuff I need to do:

. Stand up BIND9 manually
.. prove to self that you can and know what you are doing
. Design database around the needed information
.. Write database code to create tables
. write a middleware that will query the DB and write the BIND9 text files
.. query DB
.. write a uniquely named file based on date and time
.. symlink that file to BIND9 master files
.. refresh bind
.. write a script to back out changes for speed and ease of use
. Figure out if the reverse proxy goes on the server or client
.. goal here is minimal ports open on the client firewall
.. thinking client at this point 
. understand sudo
.. need to lock down what clients can do. Hoping to make a script for them to run.
.. clients don't need write access to the DB, just access to a couple of scripts stored on the server controlled by group access
. web based GUI to manage data in DB would be nice 

== Stuff done

. Picked database
.. SQLite
. Stood up Bind9 container and linked host DNS to it
.. At present Bind9 is acting as a recursive DNS